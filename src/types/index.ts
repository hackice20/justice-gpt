export interface Article {
  id: string;
  number: string;
  title: string;
  description: string;
}

export interface CaseInfo {
  incidentType: string;
  description: string;
  date: string;
  location: string;
}